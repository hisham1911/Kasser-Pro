# 🔧 حل المشاكل - PostgreSQL

## ✅ ما تم إصلاحه:

### 1. تحديث Connection String
- تم تحديث `appsettings.Development.json` لاستخدام قاعدة البيانات `kasserpro` بدلاً من `kasserpro_dev`
- الآن كل من الإنتاج والتطوير يستخدمان نفس قاعدة البيانات

### 2. تطبيق Migration
- تم تطبيق Migration بنجاح على قاعدة البيانات `kasserpro`
- تم إنشاء جميع الجداول:
  - `Stores`
  - `Products`
  - `Categories`
  - `Orders`
  - `OrderItems`
  - `Users`
  - `AppSettings`

### 3. إدراج البيانات الافتراضية
- تم إدراج البيانات الافتراضية بنجاح:
  - متجر تجريبي
  - 4 تصنيفات (مشروبات، وجبات، حلويات، مقبلات)
  - 10 منتجات
  - مستخدم admin (كلمة المرور: `admin123`)

---

## 🔍 التحقق من أن كل شيء يعمل:

### 1. في pgAdmin:

1. افتح **pgAdmin**
2. وسّع **Servers** → **KasserPro Local** → **Databases** → **kasserpro**
3. وسّع **Schemas** → **public** → **Tables**
4. يجب أن ترى الجداول التالية:
   - ✅ `Stores`
   - ✅ `Products`
   - ✅ `Categories`
   - ✅ `Orders`
   - ✅ `OrderItems`
   - ✅ `Users`
   - ✅ `AppSettings`

### 2. التحقق من البيانات:

في pgAdmin:
1. انقر بزر الماوس الأيمن على جدول `Products`
2. اختر **View/Edit Data** → **First 100 Rows**
3. يجب أن ترى 10 منتجات

### 3. تشغيل التطبيق:

```bash
cd KasserPro/KasserPro
dotnet run
```

**المنفذ الافتراضي:** `http://localhost:5108`

### 4. اختبار API:

افتح المتصفح واذهب إلى:
- **Swagger UI:** `http://localhost:5108/swagger`
- **اختبار Products:** `http://localhost:5108/api/products`

---

## ❌ إذا كانت الجداول لا تزال غير موجودة:

### الحل 1: التحقق من قاعدة البيانات

في pgAdmin:
1. تأكد أنك تنظر إلى قاعدة البيانات الصحيحة: `kasserpro` (وليس `kasserpro_dev` أو `postgres`)
2. تأكد من تحديث القائمة (Refresh) بزر الماوس الأيمن على `kasserpro` → **Refresh**

### الحل 2: إعادة تطبيق Migration

```bash
cd KasserPro/KasserPro

# حذف Migration القديم
Remove-Item -Recurse -Force Migrations

# إنشاء Migration جديد
dotnet ef migrations add InitialPostgreSQL

# تطبيق Migration على قاعدة البيانات
dotnet ef database update --connection "Host=localhost;Port=5432;Database=kasserpro;Username=postgres;Password=Postgres"
```

### الحل 3: التحقق من Connection String

تأكد من أن `appsettings.json` و `appsettings.Development.json` يحتويان على:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=kasserpro;Username=postgres;Password=Postgres"
  }
}
```

**ملاحظة:** استبدل `Postgres` بكلمة المرور الصحيحة إذا كانت مختلفة.

---

## 🚨 إذا كان التطبيق لا يعمل:

### 1. التحقق من الأخطاء:

شغّل التطبيق في Terminal وابحث عن الأخطاء:

```bash
cd KasserPro/KasserPro
dotnet run
```

### 2. التحقق من أن PostgreSQL يعمل:

```bash
# في PowerShell
Get-Service | Where-Object {$_.Name -like "*postgresql*"}
```

يجب أن يكون الحالة **Running**

### 3. التحقق من الاتصال:

```bash
# اختبار الاتصال بقاعدة البيانات
psql -U postgres -d kasserpro -h localhost
```

---

## 📝 ملاحظات مهمة:

1. **كلمة المرور:** تأكد من أن كلمة المرور في `appsettings.json` مطابقة لكلمة مرور PostgreSQL
2. **المنفذ:** تأكد أن PostgreSQL يعمل على المنفذ `5432`
3. **قاعدة البيانات:** تأكد أن قاعدة البيانات `kasserpro` موجودة في PostgreSQL

---

## ✅ التحقق النهائي:

إذا رأيت الجداول في pgAdmin والتطبيق يعمل، فكل شيء على ما يرام! 🎉

إذا استمرت المشكلة، أرسل رسالة الخطأ الكاملة وسأساعدك في حلها.

