# 🚀 دليل سريع: إعداد PostgreSQL في pgAdmin

## الخطوة 1️⃣: إضافة خادم PostgreSQL

في نافذة pgAdmin التي فتحتها:

1. **في القائمة الجانبية اليسرى:**
   - انقر بزر الماوس الأيمن على **"Servers"**
   - اختر **"Create"** → **"Server..."**

2. **في نافذة "Create - Server":**
   
   **في تبويب "General":**
   - **Name:** اكتب `KasserPro Local` (أو أي اسم تريده)

   **في تبويب "Connection":**
   - **Host name/address:** `localhost`
   - **Port:** `5432`
   - **Maintenance database:** `postgres`
   - **Username:** `postgres`
   - **Password:** كلمة المرور التي وضعتها عند تثبيت PostgreSQL
     - (إذا نسيتها، عادة تكون `postgres` للتطوير)

   **في تبويب "Advanced" (اختياري):**
   - يمكنك تركها كما هي

   **في تبويب "SSL" (اختياري):**
   - يمكنك تركها كما هي للبيئة المحلية

3. **انقر "Save"** في الأسفل

4. **سيطلب منك كلمة المرور مرة أخرى** - أدخلها

---

## الخطوة 2️⃣: إنشاء قاعدة البيانات

بعد إضافة الخادم بنجاح:

1. **في القائمة الجانبية:**
   - وسّع **"Servers"** → **"KasserPro Local"** (أو الاسم الذي اخترته)
   - وسّع **"Databases"**

2. **انقر بزر الماوس الأيمن على "Databases"**
   - اختر **"Create"** → **"Database..."**

3. **في نافذة "Create - Database":**
   
   **في تبويب "General":**
   - **Database:** `kasserpro`
   - **Owner:** `postgres` (يجب أن يكون موجود بالفعل)

   **في تبويب "Definition" (اختياري):**
   - يمكنك تركها كما هي

4. **انقر "Save"**

✅ الآن لديك قاعدة بيانات `kasserpro` جاهزة!

---

## الخطوة 3️⃣: إنشاء قاعدة بيانات للتطوير (اختياري)

كرر الخطوة 2 لكن استخدم:
- **Database:** `kasserpro_dev`

---

## الخطوة 4️⃣: تشغيل المشروع

افتح **Terminal** أو **PowerShell** في مجلد المشروع:

```bash
# الانتقال لمجلد المشروع
cd KasserPro/KasserPro

# استعادة الحزم (لإضافة حزمة PostgreSQL)
dotnet restore

# حذف Migrations القديمة (لأنها كانت لـ SQLite)
# احذف مجلد Migrations يدوياً أو استخدم الأمر التالي:
Remove-Item -Recurse -Force Migrations

# إنشاء Migration جديد لـ PostgreSQL
dotnet ef migrations add InitialPostgreSQL

# تطبيق Migration على قاعدة البيانات
dotnet ef database update

# تشغيل المشروع
dotnet run
```

---

## ✅ التحقق من النجاح

1. **في pgAdmin:**
   - وسّع `kasserpro` → `Schemas` → `public` → `Tables`
   - يجب أن ترى الجداول: `Products`, `Orders`, `Categories`, `Users`, إلخ

2. **في المتصفح:**
   - افتح: `http://localhost:5299/swagger`
   - جرب endpoint `/api/products` للتأكد من أن كل شيء يعمل

---

## 🔧 حل المشاكل

### ❌ "password authentication failed"
- تأكد من كلمة المرور في pgAdmin مطابقة لكلمة مرور PostgreSQL
- تأكد من كلمة المرور في `appsettings.json` مطابقة أيضاً

### ❌ "could not connect to server"
- تأكد أن خدمة PostgreSQL تعمل:
  - اضغط `Win + R` → اكتب `services.msc`
  - ابحث عن `postgresql-x64-16` (أو الإصدار الخاص بك)
  - تأكد أنها **Running**

### ❌ "database does not exist"
- تأكد أنك أنشأت قاعدة البيانات `kasserpro` كما هو موضح في الخطوة 2

---

## 🎉 تهانينا!

الآن مشروعك جاهز للعمل مع PostgreSQL! 🚀

