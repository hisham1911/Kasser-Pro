# 🔍 حل مشكلة خطأ 500 عند إنشاء الطلب

## الخطوات للتحقق من الخطأ:

### 1. تحقق من سجلات الخادم (Backend)

شغّل التطبيق في Terminal وابحث عن رسالة الخطأ:

```bash
cd KasserPro/KasserPro
dotnet run
```

عند محاولة إنشاء طلب، ستظهر رسالة الخطأ في Terminal.

---

### 2. تحقق من Console في المتصفح

افتح **Developer Tools** (F12) → **Console** وابحث عن:
- رسائل الخطأ من Frontend
- رسائل الخطأ من Network tab

---

### 3. الأسباب المحتملة:

#### أ) مشكلة في Token/المصادقة:
- **الخطأ:** `StoreId not found in token`
- **الحل:** تأكد من تسجيل الدخول بشكل صحيح

#### ب) مشكلة في قاعدة البيانات:
- **الخطأ:** `Connection failed` أو `Database error`
- **الحل:** تأكد من أن PostgreSQL يعمل وأن Connection String صحيح

#### ج) مشكلة في البيانات المرسلة:
- **الخطأ:** `Items is null` أو `Invalid data`
- **الحل:** تأكد من أن السلة تحتوي على منتجات

#### د) مشكلة في DateTime:
- **الخطأ:** `timestamp with time zone literal cannot be generated`
- **الحل:** تم إصلاحه بالفعل باستخدام `DateTime.UtcNow`

---

## 🔧 الإصلاحات المطبقة:

✅ تم إصلاح `DateTime.Now` → `DateTime.UtcNow`
✅ تم إضافة validation للبيانات
✅ تم إضافة logging أفضل للأخطاء
✅ تم إصلاح localStorage في Frontend

---

## 📝 للتحقق من الخطأ الفعلي:

1. **شغّل Backend:**
   ```bash
   cd KasserPro/KasserPro
   dotnet run
   ```

2. **افتح المتصفح → Developer Tools → Network tab**

3. **حاول إنشاء طلب**

4. **انقر على الطلب الفاشل (`/api/orders`) → Response tab**

5. **ستجد رسالة الخطأ الكاملة هناك**

---

## 🚨 إذا استمرت المشكلة:

أرسل:
- رسالة الخطأ الكاملة من Terminal (Backend)
- رسالة الخطأ من Network tab في المتصفح
- محتوى Request من Network tab

---

**ملاحظة:** تم إضافة logging في `OrdersController` - ستظهر تفاصيل الخطأ في Terminal عند حدوثه.

